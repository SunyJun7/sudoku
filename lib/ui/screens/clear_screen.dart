import 'package:flutter/material.dart';

class ClearScreen extends StatelessWidget {
  const ClearScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('클리어 화면 (구현 예정)'),
      ),
    );
  }
}
